import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#1170FF",
    },
    secondary: {
      main: "#8bc34a",
    },
    background: {
      default: "#f5f5f5",
      paper: "#fafafa",
    },
    divider: "#b0bec5",
  },
});
