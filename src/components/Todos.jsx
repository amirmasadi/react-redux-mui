import { Box, Typography } from "@mui/material";

export default function Todos(){
    return(
        <Box sx={{flex: 7, backgroundColor: "violet"}}>
            <Typography variant="h2">this is todos.</Typography>
        </Box>
    )
}