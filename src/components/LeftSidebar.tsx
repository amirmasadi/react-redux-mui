import { FC } from "react"
import { Box } from "@mui/material";

const LeftSidebar: FC = () => {
  return (
    <Box sx={{ visibility: "hidden" }}>
      <h1>could be another sideBar</h1>
    </Box>
  );
};

export default LeftSidebar;
